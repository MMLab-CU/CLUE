language: cpp

os:
  - linux

compiler:
  - gcc
  - clang

notifications:
  email: false

before_install:
  - source ./install-deps.sh

script:
  - cd ./lindahua/cppstdx
  - mkdir build && cd build
  - echo "CXX=${CXX}"
  - "cmake .. && make && ctest ."
