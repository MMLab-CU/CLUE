language: cpp

os:
  - linux

compiler:
  - gcc
  - clang

notifications:
  email: false

before_install:
  - source ./install-deps.sh

script:
  - echo "CXX=${CXX}"
  - mkdir build; pushd build;
  - "cmake .. && make && ctest ."
  - popd;
