language: cpp

os:
  - linux

compiler:
  - gcc
  - clang

notifications:
  email: false

before_install:
  - sudo apt-get install cmake
  - sudo apt-get install libgtest-dev

script:
  - mkdir build; pushd build;
  - "cmake .. && make && ctest ."
  - popd;
